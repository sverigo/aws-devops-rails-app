version: 0.2

phases:
  install:
    commands:
      - sudo pip install awscli

  build:
    commands:


  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'